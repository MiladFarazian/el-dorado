# Mechanics round 5 evidence — 2026-09-13 (D-060)

- `hand_before.png` → `hand_after.png` — the review `hand` view: straight fingers (a paddle) before; a relaxed curl after (proximal ~15°, middle ~40° toward the palm).
- `face_before.png` → `face_after.png` — the review `face` view: the lip tint deepened and widened; the crowns were always there, the colour now confirms them.
- `brawl.png` — the probe's brawl moment. Honest read: the soft lock turns Book onto the brawler and the chase camera re-centres behind Book, so the brawler is behind his shoulder in this plate; it proves the stance and the fists-up state, not the arm pose. The arm pose is in `pedestrians._brawl_arms` and visible in play; a side vantage in the probe is owed.
- `mech_probe_headless.log` / `mech_probe_windowed.log` — 45/45 both ways on the round's tree (the curled hands, the brawler arms and the honk changed nothing the probe measures).
- `gate_quick.md` — 5/5, 85 scripts.
- Not plated: the traffic honk (needs a car behind the player at a light — QA by hand) and the frontage wear (no vantage frames a frontage lane at grade; counts went 230 → 390 lids, 460 → 790 patches).
