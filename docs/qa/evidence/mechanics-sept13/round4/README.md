# Mechanics round 4 evidence — 2026-09-13 (D-059): Milad's three notes

- `drift_before.log` — probe stage 0 on the unfixed tree: a parked wrecker crept 3.270 m in 8 s, settling at 0.500 m/s. `drift_after.log` — 0.000 m with the park hold.
- `mech_probe_headless.log` — the full probe on the round's tree, 45/45 (the park hold did not disturb the cruiser pull-alongside, the drag test, or the brawl).
- `gait_before.png` → `gait_after.png` — the same review view: arms straight and a shuffle before; arm swing with a bent forward elbow and torso twist after.
- `full_before.png` → `full_after.png` — the standing view: the A-pose before; the idle after (elbows bent, shoulders forward, slight head turn). The review tool now runs 90 steps of the idle before every standing plate.
- `street_north_before.png` → `street_north_after.png` — the downtown roadway before and after the wear layer: tar patches along the lanes, a lid mid-lane. (`street_detail_*` frames a crossing, where the layer places only kerb drains; it is kept for the record and shows no change.)
- `plates_diff_vs_morning.md` — the 59-vantage diff against this morning's sweep with the null control: the flagged vantages are the character views (the animator) and the car views (cars used to creep during the sweep).
- `gate_quick.md` — 5/5, 85 scripts; the smoke line moved to `517.631836 / 40.4m` (the truck no longer creeps before the throttle).
