# Mechanics round 2 evidence — 2026-09-12 (D-057)

- `mech_probe_headless.log` / `mech_probe_windowed.log` — `--mech-probe`, 36/36 both ways. Stage 4 is the cruiser path: placed 40 m behind a still truck at one star it parks at 5.2 m (0.03 m/s), heat still 1, Book busted in 8.3 s, $150 fine, wakes on the impound lot. Stage 5: a favor owed → the card carries the favor line, $0 fine, favor spent. Stage 6: `spawn_now` places one dead sedan + one driver + a beacon; at TTL they despawn.
- `wanted_probe.log` — the D-044 pursuit probe still passes with the pull-alongside in.
- `brake_night.png` — 22:00, brake pedal down: tail lamps flared, both headlamp spots lighting the road (the plate D-120 owed).
- `busted.png` — the cruiser parked behind the truck, lightbar on, the BUSTED card up (fine $150, one star).
- `full_eight.png` — the veil and the rope, ~1 real-second into the ride.
- `gate_quick.md` — 5/5 on the round's tree (84 scripts).
