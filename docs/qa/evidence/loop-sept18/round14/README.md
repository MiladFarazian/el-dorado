# Alive — round 14, 2026-09-18 (D-069)

The loop's open list from round 13, closed in one round by three builders in parallel: traffic
on the atlas's roads (`traffic.gd`, spurs), people where the districts are (`pedestrians.gd`,
zones), the phone (`phone.gd`), and Wade at his door (`dealer.gd`, producer).

- `mech_probe.log` — the headless probe with stage 13 (five spur routes; spur shells and
  boulevard people after 40 s parked on Juárez) and the phone rows inside stage 11.
- `plates/order_push.png` / `plates/order_debtor.png` — taken in round 16's undisturbed windowed run. (Originally owed: the windowed probe carries the hooks (stage 11)
  but this evening's windowed run was disturbed at stage 0 (the parked wrecker drove 36 m at
  13 m/s, and the brawler never engaged at stage 8 — the same signature as the pause menu opening
  mid-sweep earlier: a keyboard at the focused window; the headless probe passed 84/84 twice).
  Re-taken with `--mech-probe --mech-shots=DIR` in round 16.)
- `plates/boone_wade.png` — the pitchman at the showroom door. `plates/cliff_boulevard.png` —
  the boulevard from the sweep (a fresh boot with the player downtown: spur traffic and zone people
  only spawn within the ring, so the plate shows the street, and the probe proves the life).
- `plates/map.png` — Boone Trucks (7) and the Cliff's places on the pause map.
- `gate_quick.md`, `shot.log` — the gate and the 67-plate sweep on this tree.
- `builders/` — the three findings files (traffic, peds, phone).
