# Feel — round 16, 2026-09-18 (D-071)

- `mech_probe.log` — the headless probe: the loop's cues (count and an on-demand play) inside
  stage 11; the takeover crowd count at SLIDE OUT in stage 10.
- `gate_quick.md` — the gate on this tree.
- `builders/` — the two findings files (loop audio, the crowd).
- `plates/order_push.png`, `plates/order_debtor.png` — the windowed probe's plates of a live order (the two owed since round 14; this run was undisturbed: 94/94 windowed, `mech_probe_windowed.log`).
- `plates/takeover.png` — the club on the lot at the takeover.
- Perf: skipped again — load 5.6 at the attempt (the Virtualization VM).
